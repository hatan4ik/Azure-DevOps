name: PR Guardian

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  pr-guardian:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - name: Run PR Guardian placeholder
        run: echo "Call internal PR Guardian API with PR diff, repo metadata and identity context."
